# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python

# Virtual environments
.venv/
venv/
env/

# EasyOCR model cache
~/.EasyOCR/

# Temp screenshots (generated by save_template.py — full window captures before cropping)
templates/current_game_state.png
templates/screenshot_*.png

# Backup files
*.bak

# macOS
.DS_Store
.AppleDouble
.LSOverride

# IDE
.vscode/
*.swp
*.swo

# Debug PNGs are KEPT (real annotated captures — useful reference for the other machine)
# bundle_dumps/ is KEPT (extracted game data — useful reference for the other machine)

# mitmproxy traffic captures (potentially contain auth tokens — never commit)
rox_traffic/
